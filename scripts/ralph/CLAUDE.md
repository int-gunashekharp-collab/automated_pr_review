# Moved

The canonical Ralph prompt is `scripts/ralph/prompt.md` (agent: Gemini CLI
on Vertex). If you ever run an agent that auto-reads CLAUDE.md, do this:
read `scripts/ralph/prompt.md` and follow it exactly.
